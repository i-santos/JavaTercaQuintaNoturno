package atividade5;

public class Calculadora {

    // métodos NÃO static
    
    // recebe 2 valores do tipo int como parâmetro
    // retorna valor do tipo int
    int soma(int x, int y) {
        return x + y;
    }
    // recebe 2 valores do tipo int como parâmetro
    // retorna valor do tipo int
    int sub(int x, int y) {
        return x - y;
    }
    // recebe 2 valores do tipo int como parâmetro
    // retorna valor do tipo int
    int mult(int x, int y) {
        return x * y;
    }
    // recebe 2 valores do tipo int como parâmetro
    // retorna valor do tipo int
    int div(int x, int y) {
        return x / y;
    }
}
